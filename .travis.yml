language: python

services:
    - docker

before_install:
    - docker pull zhuchcn/exce-rna-pipeline:lateste

script:
    - docker run -v $(pwd):/home -it zhuchcn/exce-rna-pipeline /bin/bash -c "cd /home && snakemake -np"